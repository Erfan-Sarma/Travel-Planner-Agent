# Tavel-Planner-Agent

First Insert you API inside the .env file and then start using the Project.

An API is already inserted but if you need more service use your own APIs.

> scanaroi1.txt, scanaroi2.txt, scanaroi3.txt are the tested prompts.
 **Aslo there is the libraries have already intalled on "venv"(Virtual Environment)**
